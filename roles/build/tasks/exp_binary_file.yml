---
- name: <r_build_t_exp_binary_file> create a symbolic link to /usr/local/[s]bin
  file:
    src: /file/to/link/to
    dest: /path/to/symlink
    owner: root
    group: root
    #mode:
    state: link
