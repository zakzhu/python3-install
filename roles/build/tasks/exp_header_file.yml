---
- name: <r_build_t_exp_header_file> export header files to /usr/local/include/
  file:
    src: "{{ item }}"
    dest: "/usr/local/include/{{ item }}"
    owner: root
    group: root
    #mode:
    state: link
  loop: header_file_paths
  vars:
    header_file_paths:
      -
